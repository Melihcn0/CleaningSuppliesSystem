@model PagedResponse<ResultFinanceDto>
@{
    ViewData["Title"] = "Finans Sayfası";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";

    ViewData["DeleteFunction"] = "deletedFinance";
    ViewData["DeleteUrlPrefix"] = "/Admin/Finance/SoftDeletedFinance/";

    ViewData["MultiDeleteUrl"] = "/Admin/Finance/DeleteMultiple";
    ViewData["MultiDeleteConfirmTitle"] = "Seçilen finans kaydı çöp kutusuna taşınsın mı?";

}
<div class="p-16 bg-transparent radius-8 border-start-width-3-px mb-24 shadow-4">
    <h6 class="text-primary-light text-md mb-8">Gelir / Gider Açıklaması Örneği</h6>
    <ul class="mb-0 ps-16" style="list-style-type: disc;">
        <li>1245213 No'lu Sipariş için Ödeme Alındı</li>
        <li>Fatura #2025 için Ödeme Tahsil Edildi</li>
        <li>Ofis Kirası Mart 2025 Ödemesi</li>
        <li>Dükkan Malzeme Alımı - Kağıt, Ambalaj, Temizlik Malzemeleri</li>
        <li>Elektrik Faturası Mayıs 2025 Ödemesi</li>
        <li>Çalışan Maaşları Haziran 2025 Ödemesi</li>
        <li>Bilgisayar ve Yazılım Lisans Gideri</li>
    </ul>
    <small class="text-neutral-600 d-block mt-8">
        Not: Açıklama, işlemi net olarak belirtmeli; “ödeme geldi” veya “gider oldu” gibi belirsiz ifadeler yerine detaylı ve anlaşılır bir şekilde yazın.
    </small>
</div>

<div class="card basic-data-table">
    @Html.AntiForgeryToken()
    <div class="card-header d-flex justify-content-between align-items-center flex-wrap">
        <h5 class="card-title mb-0 mt-1">Finans Listesi</h5>
        <div class="d-flex flex-wrap align-items-center gap-2 ms-auto md-0 mt-1">
            <a asp-action="CreateFinance" asp-controller="Finance" asp-area="Admin" class="btn rounded-pill btn-info-600 radius-8 d-inline-flex align-items-center">
                <span class="material-symbols-outlined me-1 d-inline-flex align-items-center" style="font-size: 18px;">add</span> Yeni Finans Ekle
            </a>
            <button id="deleteSelectedBtn" class="btn rounded-pill btn-danger-600 radius-8 d-inline-flex align-items-center" disabled>
                <span class="material-symbols-outlined me-1 d-inline-flex align-items-center">delete</span> Seçilenleri Sil
            </button>
            <a asp-action="DeletedFinances" asp-controller="Finance" asp-area="Admin" class="btn btn-danger-600 radius-8 d-inline-flex align-items-center">
                <span class="material-symbols-outlined me-1 d-inline-flex align-items-center" style="font-size: 18px;">delete</span> Çöp Kutusu
            </a>
        </div>
    </div>
    <div class="card-body">
        <!-- Filtreleme ve sayfa başına veri dropdown'u -->
        <div class="d-flex align-items-center mb-2 gap-3">
            <!-- Arama label + input -->
            <div class="d-flex align-items-center">
                <label for="customSearch" class="me-3">Ara :</label>
                <input type="text" id="customSearch" class="form-control" placeholder="Ara..." style="width: 200px;" />
            </div>

            <!-- Sayfa başına dropdown -->
            <div class="d-flex align-items-center">
                <label for="pageSizeSelect" class="me-3">Sayfa başına :</label>
                <select id="pageSizeSelect" class="form-select w-auto select-balanced">
                    @foreach (var size in new int[] { 10, 30, 50, 100 })
                    {
                        if (Model.PageSize == size)
                        {
                            <option value="@size" selected>@size</option>
                        }
                        else
                        {
                            <option value="@size">@size</option>
                        }
                    }
                </select>
            </div>
        </div>
        <div class="table-responsive" style="overflow-y: hidden;">
            <table class="table table-hover mb-0" id="dataTable" style="width: 100%; margin-top: 10px;">
                <thead>
                    <tr>
                        <th class="text-start no-sort">
                            <input type="checkbox" id="selectAll" class="form-check-input" /> #
                        </th>
                        <th>Finans Adı</th>
                        <th class="no-sort">Miktar</th>
                        <th class="no-sort">Oluşturma | Son Güncelleme Tarihleri</th>
                        <th class="no-sort">İşlem</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Data)
                    {
                        <tr>
                            <td>
                                <label class="d-flex align-items-center mb-0" style="cursor:pointer;">
                                    <input type="checkbox" class="form-check-input row-checkbox me-3" data-id="@item.Id" />
                                </label>
                            </td>
                            <td><b><u>@item.Title</u></b></td>
                            <td style="font-size:16px; line-height:2.6; min-width:200px;">
                                @if(item.Type == "Income")
                                {
                                    <div class="d-flex align-items-center gap-1 px-3 py-2 rounded-pill bg-success-100">
                                        <span class="material-symbols-outlined me-1" style="color:#388e3c;">trending_up</span>
                                        <span><b>Gelir:</b> @item.Amount.ToString("N2")₺</span>
                                    </div>
                                }
                                else
                                {
                                    <div class="d-flex align-items-center gap-1 px-3 py-2 rounded-pill bg-danger-100">
                                        <span class="material-symbols-outlined me-1" style="color:#d32f2f;">trending_down</span>
                                        <span><b>Gider:</b> @item.Amount.ToString("N2")₺</span>
                                    </div>
                                }
                            </td>
                            <td>
                                <div class="d-flex flex-column gap-1">
                                    <span>
                                        🟢 <b>Oluşturma:</b> @item.CreatedDate.ToString("dd.MM.yyyy HH:mm")
                                    </span>
                                    <span>
                                        🟡 <b>Güncelleme:</b>
                                        @(item.UpdatedDate != null
                                            ? item.UpdatedDate.Value.ToString("dd.MM.yyyy HH:mm")
                                            : "Henüz güncelleme yapılmadı")
                                    </span>
                                </div>
                            </td>
                            <td>
                                <div class="d-flex flex-nowrap gap-2">
                                    <a asp-action="UpdateFinance" asp-controller="Finance" asp-area="Admin" asp-route-id="@item.Id"
                                       class="btn rounded-pill btn-warning-600 radius-8 d-inline-flex align-items-center btn-sm action-btn">
                                        <span class="material-symbols-outlined me-1 d-inline-flex align-items-center" style="font-size: 18px;">edit</span> Düzenle
                                    </a>
                                    <button class="btn rounded-pill btn-danger-600 radius-8 d-inline-flex align-items-center btn-sm action-btn delete-btn"
                                            data-id="@item.Id">
                                        <span class="material-symbols-outlined me-1 d-inline-flex align-items-center" style="font-size: 18px;">delete</span> Sil
                                    </button>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            @if (Model.TotalPages > 1)
            {
                <div class="d-flex justify-content-end my-1">
                    <ul class="pagination d-flex flex-wrap align-items-center justify-content-end" style="gap:2px;">
                        <!-- İlk sayfa -->
                        <li class="page-item @(Model.Page == 1 ? "disabled" : "")">
                            <a class="page-link bg-primary-50 text-secondary-light fw-medium border-0 px-16 py-8 d-flex align-items-center justify-content-center h-40-px w-40-px rounded-pill rounded-end-0"
                               href="@Url.Action("Index", new { page = 1, pageSize = Model.PageSize })">
                                <iconify-icon icon="ep:d-arrow-left" class="text-lg"></iconify-icon>
                            </a>
                        </li>

                        <!-- Önceki -->
                        <li class="page-item @(Model.Page == 1 ? "disabled" : "")">
                            <a class="page-link bg-primary-50 text-secondary-light fw-medium border-0 px-16 py-8 d-flex align-items-center justify-content-center h-40-px w-40-px"
                               href="@Url.Action("Index", new { page = Model.Page - 1, pageSize = Model.PageSize })">
                                <iconify-icon icon="iconamoon:arrow-left-2-light" class="text-lg"></iconify-icon>
                            </a>
                        </li>

                        <!-- Sayfa numaraları -->
                        @for (int i = 1; i <= Model.TotalPages; i++)
                        {
                            <li class="page-item @(i == Model.Page ? "active" : "")">
                                <a class="page-link bg-primary-50 text-secondary-light fw-medium border-0 px-16 py-8 d-flex align-items-center justify-content-center h-40-px w-40-px @(i == Model.Page ? "bg-primary-600 text-white" : "")"
                                   href="@Url.Action("Index", new { page = i, pageSize = Model.PageSize })">@i</a>
                            </li>
                        }

                        <!-- Sonraki -->
                        <li class="page-item @(Model.Page == Model.TotalPages ? "disabled" : "")">
                            <a class="page-link bg-primary-50 text-secondary-light fw-medium border-0 px-16 py-8 d-flex align-items-center justify-content-center h-40-px w-40-px"
                               href="@Url.Action("Index", new { page = Model.Page + 1, pageSize = Model.PageSize })">
                                <iconify-icon icon="iconamoon:arrow-right-2-light" class="text-lg"></iconify-icon>
                            </a>
                        </li>

                        <!-- Son sayfa -->
                        <li class="page-item @(Model.Page == Model.TotalPages ? "disabled" : "")">
                            <a class="page-link bg-primary-50 text-secondary-light fw-medium border-0 px-16 py-8 d-flex align-items-center justify-content-center h-40-px w-40-px rounded-pill rounded-start-0"
                               href="@Url.Action("Index", new { page = Model.TotalPages, pageSize = Model.PageSize })">
                                <iconify-icon icon="ep:d-arrow-right" class="text-lg"></iconify-icon>
                            </a>
                        </li>
                    </ul>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.getElementById('pageSizeSelect').addEventListener('change', function() {
            const pageSize = this.value;
            window.location.href = '@Url.Action("Index", "Finance", new { area = "Admin" })?page=1&pageSize=' + pageSize;
        });
    </script>
}
